# 前置き(cache prefix)が壊れる場所の実測 — 2026-08-25 / イージス研究室

発注= 研究室HQ `DISPATCH-aegis-gl-1787583116381`(Chamiの問い=「**今までと変わらない運用で**
トークンの膨大な消費を対策できるのか?」)。台帳= `00_AI-HQ/status/hq_open_items.md` HQ-0213。

## §1 計器の穴を先に塞いだ(HQ-0213-A・commit `ff02f94`)

記録(`~/.claude/projects/**/*.jsonl`)は**1つの返答を content ブロックごとに複数行へ割って**落とす。
割れた行は `usage` を**丸ごと同じ値**で持つ。行のまま足すと2〜10重に数える。

- 直近24h・`floor_burn.collect_raw` の母集団= 行 **7,589** / 返答(`message.id`単位) **3,800**(最大**10行**)。
- 割れた **2,725件のうち 2,724件は全行の usage が同一**。
- 水増し= **書込 +60.2%**(49,352,234→19,647,671)/ **読込 +47.8%**(762,538,842→398,228,389)。
- 倍率は部屋ごとに違う(改修α 2.01 / イージス 1.78 / 人事 2.30 / llm-edu 2.43)= **順位も動く**。
- ★**HQの「書込の97.8%が1時間TTL」は数え直しても生きる**= 同じ窓で 88.9% → **90.1%**。
- ★`quota_burn.py` は「最初の1行を採る」が、**2,725件中1件**だけ usage が不揃い(片方が全部0)。
  0を先に掴むと返答が丸ごと消えるので `floor_burn.py` は**大きい方を採る**。
- 検査= `tests/test_floor_burn_split_rows.py` 17件PASS + must-fail 2本が赤。

★★**`<synthetic>` は API を呼んでいない差し込み行**。モデル欄に入るので、除外しないと
「opus→synthetic→opus」が**モデル切替2回**に見える(最初これで誤った)。数える時は必ず外す。

## §2 犯人は「往復の途中」ではなく「便の境目」だった(HQ-0213-B)

直近24h・`message.id`で重複を落とし `<synthetic>` を除外。計器= `local/_work/probe_turn_boundary_20260825.py`。

| 区分 | 本数 | 書込 | 1本あたり |
|---|---|---|---|
| ★**境目(新しい便の1発目)** | 295 | **10,753,871 (54.6%)** | 36,453 |
| 途中(道具の往復) | 3,507 | 7,399,776 (37.6%) | **2,110** |
| モデルが変わった直後 | 24 | 1,530,211 (7.8%) | 63,758 |

- ★**HQの見立て(「同じターンの中の往復と往復の間で外れている」)は主犯ではない。**
  往復は1本2,110で**健全な継ぎ足し**だ。
- 燃えているのは**便と便の境目**で、そこは**会話ぜんぶを書き直している**。
  実物= 人事部門 10:02:44 は 書込 **73,248** / 読込 **9,030**。直前の便の総文脈が 73,246
  (`prev_cr` 72,712 + `prev_cc` 534)= **ぴったり全部作り直し**。
- 境目で生き残った読込は**3つの定数に張り付く**= 9,030(100本)/ 10,001(31本)/ 0(25本)。
  295本中162本(54.9%)が20,000以下 = **システム側の塊だけ残り、会話の1通目から後ろが全部書き直し**。
- モデル切替の実額= opus-4-8↔sonnet-5 が9往復・opus-5↔sonnet-5 が7往復。**キャッシュはモデルごと**
  なので切り替えた瞬間に全書き直し。★本流とサブエージェント(`isSidechain`)は**別勘定**にする
  (混ぜるとサブが挟まるたび本流が切替に見える)。

## §3 実験で潰した筋書き(全部ハズレ)

作業ツリーは触らず、前後で HEAD と `git status --porcelain` の指紋を取って有効性を確認している。

| # | 筋書き | 結果 |
|---|---|---|
| ① | `--resume` だから壊れる | **×** resume→resume は 書込 **57〜245** / 読込 50,068 でほぼ無料 |
| ② | 道具(Read/Bash)を使うと壊れる | **×** 書込 245 / 175 で継ぎ足しだけ |
| ③ | 便が巨大で印(breakpoint)が押し出される | **×** **41,134** 書いた次の便が 書込 **56** / 読込 90,957 |

## §4 ★当たった機構= 注入ファイルが1行変わると、そこから後ろが全部書き直し

他室を巻き込まない**使い捨ての作業場**(tempdir + そこだけの `CLAUDE.md`)を立てて撃った。
計器= `local/_work/probe_prefix_claudemd_20260825.py`。

```
1〜3) CLAUDE.md 固定           → 書込 12,451 / 12,500 / 57   読込 21,148 / 21,148 / 33,648
4)    ★CLAUDE.md を1行増やす   → 書込  9,763               読込 24,028
5〜6) 据え置き                 → 書込     57 /     57      読込 33,791 / 33,848
```

`claude -p` は**便ごとに別プロセス**で前置きを組み直すので、注入物が1バイトでも動けば
**そこから後ろが死ぬ**。2026-08-23に `gitStatus` で見つけたのと**同型**
(対処= `CLAUDE_CODE_DISABLE_GIT_INSTRUCTIONS=1`・`session_relay._child_env()`)。

## §5 ★まだ言えないこと(推測で埋めるな)

**本番で「毎便変わる注入物」が何かは特定できていない。**候補を実物で当たったが全部空振り=

| 候補 | 最終更新 | 判定 |
|---|---|---|
| `5SecMovieMaker/CLAUDE.md` | 2026-08-13 | 295回の説明にならない |
| `.claude/settings.json` / `settings.local.json` | 2026-07-21 | 同上 |
| `~/.claude/CLAUDE.md` | 2026-07-14 | 同上 |
| メモリ(`memory/*.md`) | 直近24hで2件 | 同上 |

- ★**`claude --debug-file <path>` は送った本体を吐かない。**実際に撃って読んだ= 出るのはプロセスの
  動作記録(MCP接続・一時ファイル・パイプ名)だけ。時刻とIDを伏せて差分を取っても残るのは
  パイプ名とPIDだった。**HQの言う「前置きを2つ取り出して差分」はCLIの記録からは取れない。**
- ★**`ANTHROPIC_BASE_URL` を差し替えて記録する手は使わない**(2026-07-26の口座事故と同じ形)。

## §6 次の一手(この順で)

1. ~~**モデル切替の実額 1,530,211/24h(全書込の7.8%)を削る。**~~ **→ やった(commit `1bafcd3`)。**
   ★**上限は「回数」ではなく「採算」に置いた。**回数の上限(1時間にN回まで等)を実データで当てると、
   **一番の黒字(+1,561,816の1件)を先に切ってしまう**= 切替は高いのではなく、**短い便への寄り道**が高い。
   - 寄り道1回の固定費= 落ちる時の書込(Sonnet価格 平均75,349)+ 戻る時の書込(**Opus価格** 平均66,956)
     = 重み付きで約 **30万**。Sonnetの節約はOpus便の6割しかないので、**元が取れない便は下げるほど高い**。
   - 24hの寄り道12件の採算= 黒字4 / **赤字5(合計 -1,340,314)** / ±小3。
     ★赤字5件は**全部37秒以下**(10/13/18/30/37秒)、黒字は65秒以上(65/173/404/523)= **分水嶺は60秒**。
   - ★**②(work格下げ)全体の24hの節約は +552,779(重み付き)= 総燃焼の約0.3%しかない**。
     しかもその全部が**当たり1件**で説明できる。赤字を止めれば同じ運用のまま約3倍になる。
   - 入れた物= `work_audit.jsonl` に `author`(追加のみ)/ `work_relay_decide` の5つ目の条件
     `_switch_pays_off`(部屋×差出人の秒数の中央値 ≥ `min_work_sec`=60)/ 理由の語 `too_short`・`no_history`。
     `min_work_sec: 0` の1行で無効化できる。検査= `tests/test_model_switch_cap.py`(35件+must-fail2本)。
   - ★計器= `local/_work/probe_model_flap_20260825.py`。★**セッションファイル1本=1つの会話**として数える
     (部屋名で束ねて時刻順に繋ぐと、世代交代の境目が「切替」に化けて23回に盛れる。最初これで誤った)。
   - ★**まだ「直った」ではない**= `work_model_check.py --hours 24` の内訳に `too_short`/`no_history` が
     実際に立つのを見るまで。履歴は今夜から貯まる(古い行には `author` が無い=数えない)。
2. **境目の全書き直しの特定**= 本番セッションへ**同じ便を2回流して読込の差を取る**しか手が無い。
   本番の部屋を汚さない撮り方を先に決めてから撃つ。**未着手。**
   - ★**撃つ前に潰しておく罠(研究室HQ実測 `DISPATCH-aegis-gl-1787585184396` の①)**=
     `claude -p --output-format json` の**集約後の `usage` は反復ぶんが合算されている**。
     道具を1回でも使った便では床を反復回数ぶん重ねて持つ(同じお題で iterations[0]=54,384 に対し
     集約後 109,243= **ちょうど2倍の嘘**)。**`usage.iterations[0]` を読み、`len(iterations)` が
     1でない測定は捨てる。** `cache_prefix_probe.py` は直した(commit `43819e8`)。
   - ★HQの指定= 新しい仕組みを作る前に既存の **`"test": true`** を先に試す。
     ただし実物を読んだ限り `"test": true` が止めるのは **Discordへの送信だけ**で、
     便は本番セッションへ普通に入る(`dept_daemon.py` :6099 / :6398 等)。
     = **「部屋を汚さない」は満たさない。**2発目は1発目を文脈に含んだ状態で撃つことになるので、
     読込の差にその分が乗る。**この扱いを決めてから撃つこと**(捨てセッションを1本作る等)。

## §7 計器の在りか(全部 `5SecMovieMaker/local/_work/`・`local/` はgitignore)

`probe_floor_dup_20260825.py`(二重勘定の実測)/ `probe_turn_boundary_20260825.py`(境目 vs 往復)/
`probe_cache_miss_head_20260825.py`(頭から作り直したか末尾の継ぎ足しか)/
`probe_miss_neighbors_20260825.py`(外れた便の前後を記録から取り出す)/
`probe_prefix_diff_20260825.py`(`--debug-file` の差分)/ `probe_prefix_bisect_20260825.py`(道具の切り分け)/
`probe_prefix_bigletter_20260825.py`(便の大きさ)/ `probe_prefix_claudemd_20260825.py`(★当たり)
