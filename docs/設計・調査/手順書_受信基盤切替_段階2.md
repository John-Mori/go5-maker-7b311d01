# 手順書: 受信基盤切替(段階2) — 旧鳩jsonl → Gateway+LeaseQueue

- 日付: 2026-07-18 起草(研究室)・QAレビュー対象
- 前提文書: 改善設計書_基盤恒久化_世界OSS調査_2026-07-18.md §4 / QA: 受け入れ条件表_段階2切替.md
- 実施条件: **QA Release Gate 4条件クリア+Chami実況(Discordで見ている時間帯)**。パイロット第1号=data-org箱。

## 0. Gate状態(2026-07-18時点)

| 条件 | 状態 |
|---|---|
| 1 周期ジョブ移植 | ✅ QA実装済(2f13575・GO5_GATEWAY_JOBS=1まで完全停止) |
| 2 両肺同時ロールバック | 本書§4に手順化。★B2=**カットオーバー前にロールバックを1度実際に通す**(§2リハーサル) |
| 3 consumer | ✅ 研究室実装済(56e1a5a・waiterデュアル監視=WAITER:MESSAGE契約不変・fail-open) |
| 4 代打の旧判定 | ✅ 解消済(739a059時点で2信号判定が本番稼働) |

### 裁定: QA発見の未移植2点(2026-07-18・研究室)
- **A2 送信印(📮)= 移植する**。3段印はChami設計のUX(未達/届いたが無人/読んだ/着手の4状態判別)であり切替で退化させない。gateway on_messageで押す。並走中は鳩と二重押しになるが、**同一Bot・同一絵文字のリアクションはDiscord側で1個に収束=無害**。実装はjobsゲート(GO5_GATEWAY_JOBS)内に置き、シャドウ無副作用の原則を維持。
- **A10 チャンネル名自動追従 = 日次で移植**。job_prune(日次)に同居。低頻度で十分・ch追加そのものはGatewayがguild購読なので再起動不要(旧鳩より改善)。

## 1. 事前準備(停止なし・いつでも可)
0. ★**代打のqueue読み対応(研究室・切替の必須前提)**: 切替後はjsonlに新着が来ない=旧代打は盲目になり「絶対応対の最後の受け皿」が消える。claude_responderへ「研究室死亡時にqueueのrouter/research-room/main宛てをclaim→--print処理→ack」を実装してから切替する(手順書起草時に研究室が発見・§6の「要確認」を必須へ格上げ)
1. A2/A10をgatewayへ実装(QA)→ selftest追加
2. 旧jsonl台帳の完全バックアップ実行(backup_local_to_drive.ps1 手動1回)
3. シャドウqueueのpending照合(研究室・実施済2026-07-18: pending 0件)。>0なら処理済み台帳と突合し、照合済みをack(result='shadow照合')・未照合はChamiへ提示
4. 改名案件(データ整理)の統合可否を確定(→§5)。未確定なら改名なしで実施し、改名は別窓に切り離す

## 2. リハーサル(B2・カットオーバー前日〜直前)
1. data-org箱だけでGO5_GATEWAY_JOBS=1を仮点灯→45秒受領印・30分エスカレの動作をダミーmsgで確認→消灯
2. **ロールバック演習**: §4の手順を実際に1周通す(gateway停止→鳩継続を確認→再度gateway起動)。所要と詰まりを記録
3. 受け入れ条件表の「切替前」列をQAが埋める(現行値の採取)

## 3. カットオーバー(計画停止・Chami実況・想定30分)
> 実況: 各ステップ完了ごとにhr-room chへ1行(アメス)。異常があれば即§4。

1. **[T-0]** Chami在席確認→開始宣言(Discord)
2. 全部門セッションへ周知(SendMessage): 「切替中・新規作業の着手を10分保留」
3. **gateway常駐化**(supervise_daemons.ps1へ登録・python絶対パス=データ整理指摘)+GO5_GATEWAY_JOBS=1
4. パイロット: data-org箱をqueue経路へ(鳩のdata-org配達を停止 or dept購読から除外)→ダミー1発で E2E(Gateway受信→queue→waiter鳴動→claim→ack→📮/受領印)を実測
5. パイロットOK(受け入れ条件表の該当行PASS)→ **鳩の配達を全部門停止**(鳩プロセス停止。※absence_watchdog/local_responderは継続)
6. 全部門waiterは既にデュアル監視=そのまま。研究室がstale_pending/rerouteの初回巡回を手動1回
7. 受け入れ条件表「切替後」列を全行チェック(QA)。P1受領印/P4添付/sweep(reroute)/📮/遅延2秒以内。タスク検証はreregister_tasks.ps1の`-Fire`でrc=0確認(※タスクモードのスクリプトは常に0を返す設計へ統一済=データ整理2026-07-18。警告はレポートファイルが信号・終了コードは健康状態のみ。常駐系はFireAll禁止=再開フェーズで各自の手順から)
8. 完了宣言(Discord)+system_changesへCHG記録+本書§0を更新

## 4. ロールバック(両肺同時・片肺禁止)
> 発動基準: 受け入れ条件表のいずれかがFAIL・配達遅延>60秒・queue異常(SQLITE_BUSY頻発等)。判断=研究室、宣言はDiscordで。

1. GO5_GATEWAY_JOBS=0(ジョブ停止)→gateway常駐停止
2. **同じ手で**鳩を再起動(start_discord_inbox.bat)=producer/consumer両方を旧経路へ同時復帰
3. waiterはデュアル監視のまま=jsonl側で鳴る(改修不要・これが片肺事故を防ぐ設計)
4. queueに残ったpendingを処理済み台帳と突合→未処理分は手動でmain箱jsonlへ書き戻し
5. 原因をインシデント.mdへ・再挑戦はGate再チェックから

## 5. 改名の統合(データ整理実測2026-07-18で確定・正本=改名移行台本_5SecMovieMaker.md)
- **対象**: `D:\SougouStartFolder\go5-maker` → `D:\SougouStartFolder\5SecMovieMaker`(Chami承認7/16)。**公開URLは不変**(リポジトリ名は既に別名でPages紐付け済・データ整理確認済)
- **実測影響**(凍結時に再実測を最終版とする=切替日までに増えるため。再実測はgrep+Get-ScheduledTaskで3分): 実行系約22ファイル(_daemons ps1×8・start系bat×8・maintenance・sla・claim_lab・起動bat等)+文書系約14(停止不要)+**スケジュールタスク11本**+Claude memory dir引っ越し+Obsidian開き直し(1分)
- **★順序制約(データ整理発見)**: git worktreeのgitdirリンクは絶対パスの相互参照=改名で両方向に壊れる。**改名はworktree分離(S5)より先**。既存worktree(`wizardly-aryabhata-19852f`)は改名前に`git worktree remove`(未コミット変更が無いことを確認してから。あれば救出→remove)
- **方式**: 事前の相対化(%~dp0/$PSScriptRoot化)はやらず、**停止中に一括置換+タスク11本再登録**(二度触らない・データ整理の提案採用)。再登録は自動化スクリプト(データ整理作成: 登録→実発火→LastTaskResult=0確認まで)。python絶対パスで登録(拡張子関連付けの黙死対策・実測済)
- **統合後の§3順序(改名込み・計画停止は1回)**:
  1. [停止前] worktree残置物の確認とremove/全窓を閉じる案内(Chami)
  2. 全常駐停止(supervise・鳩・watchdog・responder×2)+タスク11本無効化 → プロセス0件を確認
  3. **フォルダ改名** → 一括置換(実行系22)→ タスク再登録スクリプト実行 → memory dir引っ越し → claim_lab札の更新
  4. **旧構成のまま全常駐を再起動**して健全確認(改名の検証を先に完結・鳩も一旦復活)
  5. ここから§3のカットオーバー(gateway常駐化→jobs ON→data-orgパイロット→鳩停止→受け入れ条件表)
  6. セッション開き直し(新パスで起動bat→claim_lab→waiter)・Obsidian案内(データ整理)
- **所要の改訂**: 改名30〜45分+切替30分+開き直し=**計画停止は合計60〜90分**(Chami在席・実況)
- **ロールバックへの影響**: なし(改名は切替と独立。切替のロールバック=新パス上で鳩を再起動するだけ・§4のまま有効)
- 分担: 一括置換+検証=データ整理30〜45分/タスク再登録スクリプト=データ整理(事前作成)/gateway・queue系=QA/進行・実況・裁定=研究室

## 6. 切替後の後始末(当日〜翌日)
- 旧鳩コードは削除せず1週間残置(ロールバック余地)→安定後にアーカイブ
- BOOT_TEMPLATE/orchestration核の起床手順を「claim/ack」表現へ更新(waiter契約は不変のため文言の差し替えのみ)
- 代打(claude_responder)の読み先をqueueへ切替(次段・当日はjsonl読みのままでも成立=mainへの新着はqueue経由でもjsonlへ来ない→**要確認: 代打はカットオーバー当日にqueue読み対応が必要か** → §3-4のパイロットで実測判定)
- 監視: absence_watchdogのINBOX_FILE滞留監視をqueue版(stale_pending)へ移す(1週間は両方)
