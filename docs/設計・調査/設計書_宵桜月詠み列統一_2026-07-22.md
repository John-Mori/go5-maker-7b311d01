# 設計書: 宵桜艶帖→月詠み 列統一

- 作成: 2026-07-22 system-engineer
- ステータス: **実測完了。列削除はChamiの裁定待ち(裁定 C-003)**
- 関連: 設計書_投稿履歴シート正本化_2026-07-22.md / GAS 2026-07-22C

---

## §1 現状(コードから確認できる事実)

### 列定義の正本

```
CANONICAL_HEADERS = HEADERS40(33列) + FANZA_HEADERS(6列) + EXTRA_HEADERS(11列) = 計50列
```

| グループ | 列数 | 代表列 |
|---|---|---|
| HEADERS40 | 33 | post_id / 投稿日時 / 題名(コメント) / post_uri / YouTube動画URL / 短縮URL 等 |
| FANZA_HEADERS | 6 | 元値list_price / 割引後price / 割引率pct / FANZA取得日時 / レビュー件数(代理指標) / レビュー平均 |
| EXTRA_HEADERS | 11 | カテゴリ / 作品状態 / 共有URL / 作り直し / ハッシュタグ / リビルド元ID / タイトル文字数 / 目的 / コメント型 / YT補正累計 / 作品短縮URL |

---

## §2 実測結果(2026-07-22 08:34 JST)

### バックアップ完了

| シート | バックアップ名 | 行数 |
|---|---|---|
| 月詠み | 月詠み_bk_20260722_0834 | 38 |
| 宵桜艶帖 | 宵桜艶帖_bk_20260722_0834 | 999 |

### 行分類

| 分類 | 月詠み | 宵桜艶帖 |
|---|---|---|
| complete(uri有+YT有) | 18 | 13 |
| **no_yt**(uri有・YT無) | **1** | **5** |
| no_uri(uri無・YT有) | 19 | 5 |
| minimal(両方無) | 0 | 0 |
| **empty**(post_id無) | 0 | **976** |
| データ行合計 | 38 | 999 |

### ヘッダー差分

| | 月詠み | 宵桜艶帖 |
|---|---|---|
| headerCount | 50 | **52** |
| missingFromCanonical | [] | [] |
| **extraColumns** | [] | **[Bluesky投稿URL, Bitly_ID]** |

→ **1〜50列目は両シート完全一致(順序・名前)。宵桜だけ末尾に2列多い。**

### 余剰2列の中身(宵桜のみ・非空セル数)

| 列名 | 非空セル数 | 由来 | 現在の扱い |
|---|---|---|---|
| Bluesky投稿URL | **3件** | 旧フォーマット(手動入力時代) | GASコメントで「当面温存」と明記 |
| Bitly_ID | **2件** | Bitly全廃前の旧クリック管理列 | 同上。Bitly全廃済みで中身は死んでいる可能性高い |

**★削除はChamiの裁定待ち(C-003)**。3件/2件は少数だが中身が入っているため、削除前に確認要。

---

## §3 no_yt 6行の詳細(postUri有・YouTube URL無)

**★削除はしない。Chamiが起きてから裁定を取る。**

### 月詠み — 1行

| 行番号 | 題名 | rkey(末尾) | shortUrl |
|---|---|---|---|
| 6 | ワークライフバランスは大事 | 3mq7bcv6vc72j | r2.../N1OhQ |

備考: 同題名・別postUriが 行5(rkey=3mq7djhzts32s)にも存在(complete)。
→ 40分差の別投稿か、片方が重複登録かはChamiが確認。

### 宵桜艶帖 — 5行

| 行番号 | 題名 | rkey(末尾) | shortUrl |
|---|---|---|---|
| 15 | 続きが気になっちゃう一冊、みつけた📚 | 3mphu6mmg7g2b | r2.../aREKZ |
| 16 | 幼馴染みの育て方、バグってる | **3mpbqgggryp22** | r2.../pdFDN |
| 20 | 時間停止で◯◯放題‼︎ | 3moxgdvmpmt2d | r2.../1eYbH |
| 21 | 続きが気になっちゃう一冊、みつけた📚 | 3mox6lk2xzl2r | r2.../Bbb3h |
| 23 | W杯どころじゃない話！ | 3mox5tvb4gs2u | r2.../4D94k |

備考: 行15/21は同題名・別rkey(7日差) → 別実投稿。どちらもno_yt。

---

## §4 重複postUri 2ペアのYT有無

### 月詠み: rkey=3mppdo6jjoo2x (行17/18)

| 行番号 | hasYt | 判定 |
|---|---|---|
| 17 | **true** | complete |
| 18 | **true** | complete |

→ **両行ともYouTube URL有り = リビルドの正常記録。消すな。**

### 宵桜艶帖: rkey=3mpbqgggryp22 (行16/17)

| 行番号 | hasYt | 判定 |
|---|---|---|
| 16 | **false** | no_yt (§3のno_ytリスト行16と同一) |
| 17 | **true** | complete |

→ **行16が余剰行候補。ただし削除はChamiの確認後。**
  統合する場合は行16の情報を行17に吸収してから削除(メモリ: INC-112参照)。

---

## §5 列統一の方針

### 方針: reorder_headers のみ(列削除はChamiのGO待ち)

`?action=reorder_headers` はすでに実装済み(GAS line 1192)。
これを実行するとCANONICAL_HEADERSの順序通りに並び替えられ、
未定義列(Bluesky投稿URL / Bitly_ID)は末尾に寄る。

| 変更 | リスク | 承認状況 |
|---|---|---|
| `?action=reorder_headers` 実行 | 低(列名で書くため機能は列順に依存しない) | 裁定待ち |
| 余剰2列の削除 | 中(3件/2件の中身が消える) | **Chamiのシート確認後** |
| empty 976行の扱い | 別設計 C-001依存 | Chamiのシートが正か確認待ち |

**Chamiへの確認事項:**
1. 宵桜の `Bluesky投稿URL`(3件) / `Bitly_ID`(2件) は削除してよいか？
2. empty 976行は「基本的に使われていない行」の認識でよいか？(prune_history廃止前のシート掃除の残骸か)
3. 宵桜の no_yt 5行は YouTube URL がない = 投稿済みだがYT動画未登録、という認識でよいか？

---

## §6 統一作業の優先度表(実測後確定)

| 問題 | 対処 | 優先度 | 状態 |
|---|---|---|---|
| 列順の差(余剰2列が末尾外にある場合) | `?action=reorder_headers` 実行 | 低(既に末尾にある) | 裁定待ち |
| 余剰2列(Bluesky投稿URL/Bitly_ID) | CLEANUP_COLUMNS追加 → `?action=cleanup_columns` | 内容確認済み | **Chami裁定待ち** |
| empty 976行 | C-001完了後にprune_historyの代替で対処 | C-001依存 | Chami確認待ち |
| 重複postUri 2ペア | 月詠み=消すな / 宵桜行16=余剰候補(統合後削除) | 実測済み | **Chami裁定待ち** |
| no_yt 6行(YT未登録) | 手動でYouTube URLを補完するか、保留扱いか | データ補完 | **Chami裁定待ち** |

---

## §7 カテゴリ列 同人/Books(次タスク・Chami確認要)

現在のATTR_DEFS(カテゴリ属性定義):
```
chara=キャラ / jk=JK / gyaru=ギャル / isekai=異世界 / harem=ハーレム / ai=AI / ol=OL / soshu=総集編
```

追加案: `{ key: 'doujin', label: '同人' }` / `{ key: 'books', label: 'Books' }`

**Chami確認事項:**
1. `同人/Books` は既存の内容属性(キャラ/JK)と同じ `カテゴリ` 列に入れてよいか？
   → または `商品種別` という別列を作るべきか？
2. フロント(index.html)のカテゴリUIに「同人」「Books」チェックボックスを追加するか？

実装工数: Sonnet 1ターン。設計が確定してから着手。

---

## §8 並べ替え(Chami確認要)

**案A: 列の並び** = §5で解決。`reorder_headers` 実行で完了。

**案B: シートの行を投稿日時順にソート** = GASに行ソート用のactionが未実装。実装が必要。

**★Chami確認事項:** 「並べ替え」は列順か行順か？
