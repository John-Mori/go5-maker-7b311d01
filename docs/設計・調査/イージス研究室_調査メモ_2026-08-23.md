# イージス研究室 調査メモ 2026-08-23(第24〜25世代の実測の正本)

引き継ぎ(`local/llm/handoff_aegis-gl.md`)から**調査の結論**を切り出した正本。引き継ぎ側は結論1行+ここへのパスだけを持つ。

---

## 1. 床(毎便の固定費)= ③ の調査

道具= `scripts/llm/floor_burn.py`(`--by ttl` / `--by dept`)。★このファイルのdocstringが一次資料。

**死んだ仮説4つ**(全部、対照を取って殺した):

| 仮説 | 実測 | 対照 |
|---|---|---|
| セッションの寿命切れで床を書き直している | 10.0% | 普通の便 1.0% |
| 圧縮の直後に書き直している | 0.6% | — |
| サブエージェントが床を積んでいる | 2.0% | — |
| 特定の部屋が悪さをしている | 改修α12.6 / イージス11.6 / HQ11.1 | **忙しさに比例して素直に散るだけ** |

**分かったこと**= 全部屋で同じ形で起きている構造。犯人は「量」ではなく**書込の預け方**。

- 直近24時間の書込= **1時間TTL 91,755,208(97.8%・11,041便) / 5分TTL 2,053,795(2.2%・466便)**。
- ★★**決着(8/23 15:41・研究室HQ msg 1540974350445060156)= 2.0で正しい。**HQが公表価格表をその場で取って確認
  (`docs.claude.com/en/docs/build-with-claude/prompt-caching`。どのモデルも比は同じ= 5分1.25 / **1時間2.0** / 読込0.1 / 出力5)。
  → `quota_burn` は `W_CC_1H=2.0` を新設し**寿命の種別で別倍率**へ(HQ commit `ab83bd6`。`--cc1h 1.25` で旧来へ戻せる)。
  → `floor_burn` 側も**倍率を自分で持たず `q.CC1H_W` / `q.W_CACHE_CREATE` を読む**形へ直した(正本は quota_burn 1本)。
- **旧(一律1.25)は書込を 58.7% 過小**に見ていた= 実測 95,095,500×2.0 + 2,053,795×1.25 = **192,758,243**(旧 121,436,618)。
- ★★**HQ側の `MODEL_W` に2つ誤りが在って直った= opus 5.0→2.5(倍に盛っていた) / haiku 0.27→0.5。**
  = **Opus便をSonnetへ落とした時の節約は5倍でなく2.5倍**。②(work上書き)の値付けは**半分**になる。
- **順位が入れ替わる1時間TTLの倍率= 1.30**(HQ実測)= 1.25と2.0の間に境界が在った。
  → 旧の数え方のままなら「読込がわずかに勝つ」側で③の順番を決めていた。**「先に書込から入れ」は値段でも裏が取れた。**

大書込(≥3万)の便の性質= 全便の8.4%(940本)で書込の75.7%を作る / その96.9%はセッションの初回ではない / 読込の中央値が11,578(普通の便は114,989)=**前置きの前の方が変わって後ろを丸ごと書き直している** / 前便からの間隔は67%が60秒以内。

### 1.2 ★大書込は「どこで折れたか」で見ると全部屋 同じ所で折れている(8/23 追測)

道具= `local/_work/bigwrite_break.py`(測るだけ・何も書き換えない)。**cache_read = その便で生き残った前置きの長さ**なので、これを並べれば折れた位置が出る。直近24h・全便11,949・大書込989本。

- **折れる位置は6か所に集中**= 7000台25.7% / 10000台19.9% / 21000台18.9% / 25000台11.2% / 24000台8.4% / 11000台7.4%(**上位6で91.5%**)。
- ★★**この6か所は部屋が変わっても同じ**(改修α・イージス・HQ・プラットフォームSE・軍議・人事…全室で上位3が 7000/10000/11000/21000/24000/25000 台のどれか。部屋ごとの上位3で72〜91%)。
  → **部屋ごとに長さが違う起動文が原因ではない。全部屋で共通の「頭」の側で折れている。**
- **落差**= 同じ部屋の普通の便は読込 中央値 **91,000〜118,000**、大書込の便は **7,000〜21,000**。
  = 大書込1本につき**約10万トークンの前置きを捨てて書き直している**(989本で書込の75.7%と整合)。
- 前便の到達点(cr+cc)と比べた生存率= **ほぼ全滅(5〜20%)62.7% / 半分(20〜60%)29.1% / 全滅(0〜5%)7.0%**。
- ★★**全滅65本のうち53.8%は前便から60秒以内**= **寿命切れでは説明できない**(60秒で切れる寿命は無い)。

**言えること**= 大書込の主因は「量」でも「寿命切れ」でもなく、**前置きの再利用が頭の近くの数か所まで巻き戻ること**。
★**巻き戻る理由は未測定。**候補= ①同じセッションへ便が重なって走る(全滅の53.8%が60秒以内と整合) ②履歴の途中が書き換わる。**どちらかを測るまで結論にしない。**

---

## 2. GASの読み取り口が約1時間 落ちた件(経路B・8/23)

- **窓**= 2026-08-23 14:32〜15:36 JST。`teian_echo_poll` が12回連続で失敗(各25秒のtimeout)。15:37の周で**自力復旧**、`hq_open_items.md` の `teian-echo:read-fail` も RESOLVED。
- **取りこぼしゼロ**= fail-openで水位=2を据え置いたため。決定行の喪失は無い。
- **切り分け(全部 実測)**:
  - 口が無いのではない= `action=teian_decisions` は `headers`/`lastRow`/`rows` を正常に返す(復旧後 2.3〜2.9秒)。
  - DNS/TCP/IPv6ではない= `script.google.com` へのTCP接続はIPv6・IPv4とも 0.01〜0.04秒。
  - urllib固有でもUA固有でもない= 既定UA / 独自UA / ブラウザUA の3通りとも復旧後は1.3〜1.8秒で通る。
  - ★**`?ping=1` すら55秒でタイムアウトしていた**= doGet 自体が返っていない=**うちのコードの外(Google側)**。
- ★**真因は不明のまま。**「GASの同時実行が5分毎トリガー(snapshotStats・時間予算5〜6分)に埋まって待たされた」は**仮説であって未測定**。
- **残る症状**= 復旧後も約6%が25秒で刺さる(16本中1本・15:40:45)。計器= `local/_work/gas_probe_aegis.py`(25秒間隔で `?ping=1` を叩き時刻と応答時間を残す)。閾値は3回連続なので単発のフレークでは鳴らない。

---

## 3. C-054「その脈を打てる手を数える」= 絵文字一覧の脈

対象= `local/_work/reaction_watch_kaizen_digest.md`(producer名 `reaction_watch_digest` / max_age 48h / consecutive 2 / 鳴らす先 aegis-gl)。

| 手 | 種別 | 状態 |
|---|---|---|
| `scripts/discord/reaction_watch.py` | 書く(センサー本体・毎朝8時) | ★これだけが正しい手 |
| `scripts/_daemons/run_kaizen_daily_repair.py` | **読むだけ**(exists/getmtime/読み取りopen) | 問題なし |
| `tests/test_emoji_digest_pulse.py` | 旧= 本番へ書き mtimeも倒していた | **塞いだ**(一時ディレクトリへ差し替え+検査⑦) |
| **人の手** | 誤発火よけの種を1行書く運用 | ★**残っている**= ②(登録直後の猶予)が入るまで消えない |

★**打てる手が2つある=まだ見張れていない。**②の発注先はプラットフォームSE。

---

## 4. 忙しい部屋の常駐が載せ替わらない(A)— 未修理

一番忙しい5室(aegis-gl / hq / hr-room / research-room / system-engineer)が**一番載せ替わらない**。
実物= `local/_daemon_keeper.log`「処理中の便が在るので次の周回へ回す」/ `local/_reload_keeper.log`「★120分待ったが暇な窓が来なかった」。
**現時点では噛んでいない**(現codeverで32/32一致)が**構造は未修理**。

案= ①受け口だけ新プロセスへ ②120分打ち切りを警報へ ③純関数だけ `importlib.reload`。
★**3案とも未測定。測ってから決める。**
