# 調査: 投稿履歴 dupe弾きの誤紐付け——書き込み元の特定(2026-08-15・改修α/オタコン)

対象: 新規作品 jrMDq1Tx1mM を即時投稿→「二重登録」で弾かれ履歴に載らない(Chami 2026-08-15・再発🔥)。
既存の衝突相手= 背骨ID `acc1-20260813-2226-2z9y` / 題名『入る前には戻れない』が jrMDq1Tx1mM の YouTube動画IDを誤って保持。

## 1. dupe判定の実物(症状の出口)
`js/yt-clicks.js` `addCompletedPost_`(L2414〜):
- L2439 `var vid = ytUrl ? ytIdOf(ytUrl) : ''` = 新規投稿のYouTube動画ID。
- L2460 `if (vid && ytIdOf(y) === vid) { matched = it; matchedBy = 'ytUrl'; }` = **既存行のytUrlが同じ動画IDを指すと弾く。**
- L2505 dupeで `existing.ytUrl` を返す → v=790 の確認ダイアログ(`js/stock.js` L737-751)がそれを見せ、forceNewで新規追加できる止血。**止血はlive(v=797)。**

→ 出口は正しい。**入口(既存行にjrMDq1Tx1mMを書いた経路)を塞がないと、同題名の新規投稿で再発する。**

## 2. 誤紐付けの書き込み元(candidate 2本・実物照合)

### A. 題名一致の自動接続(単発一致に時刻ガードが無い)= 初発の疑い
`restoreByYtData_`(L3574-3623)。迷子YouTube URLを**正規化題名の一致**で行へ書き戻す。
- L3605 `norm(meta[v].title) === nt` で候補動画を拾う。
- L3607-3612: 候補が**複数**なら `|published − it.ts| ≤ 72h` を要求(誤接続を防ぐ)。
  **だが候補が単発(hits.length===1)だと L3613 で時刻を一切見ずに `it.ytUrl` へ束縛する。**
- 帰結: 題名(=コメント+タグで生成)が既存の未解決行と一致する**新しい**動画は、公開が何日離れていても
  その古い行へ結線される。『入る前には戻れない』の題名を再利用した新作 jrMDq1Tx1mM が古い行に付いた形と整合。
- トリガ: 自動ではない。**🔧YT情報を診断・修復ボタン(`ytRepairLinks` L3739)**を押した時だけ走る。
  (タブ入場の自動 `maybeRestoreYt_` L3446 は背骨ID鍵の `restoreYtFromSheet_` のみ=題名照合は呼ばない)

### B. シートが権威dirtyになり「復活」する= 消しても戻る真因
`restoreYtFromSheet_`(L3402-3444)。**背骨ID(videoId)鍵**でシート行の `youtubeUrl` をローカル行へ書き戻す(L3423 空の時のみ)。
- これは**タブ入場で毎回自動**(`maybeRestoreYt_`)。
- Aでローカルが汚れる→ yt-clicks.js の upsert(L1600-1620 `youtube_url` を送信)が**シート行 acc1-20260813-2226-2z9y の youtube_url に jrMDq1Tx1mM を焼き付ける**→
  以後はシートが権威。**ローカルで消して保存しても、次のタブ入場で L3423 がシートから書き戻す=「復活」**(REQ-811075a64f『消して保存しても復活』と同型)。

## 3. どちらか確定するのに要る1点(Chamiのrepro=研究室が既に依頼済み)
1. v=797でリロード→ jrMDq1Tx1mM を再投稿→ **確認ダイアログ本文**(`matchedBy` が ytUrl か videoId か / 既存の題名・背骨ID)。
2. jrMDq1Tx1mM の**YouTube題名**が『入る前には戻れない』と一致するか(=A成立の条件)。
3. 記録シートの行 `acc1-20260813-2226-2z9y` の `youtube_url` 列に jrMDq1Tx1mM が入っているか(=B=権威dirtyの確認)。

## 4. 確定後に打つ手(実装はOpus・確認前に本番投入しない)
- **A(入口)**: L3607-3613 の**単発一致にも時刻ガードを課す**(公開と行tsが72h以上離れた単発は束縛しない・it.ts欠落行は据え置き)。
  → 新作が題名衝突で古い行へ吸われるのを止める。※束縛ヒューリスティックの挙動変更=repro確定を待つ。
- **B(既存の汚れ+復活)**: 行の🛠️編集からYT URLを外す操作が**シート側の youtube_url も空へ**upsertするようにし、
  L3423 の書き戻し対象から外す(空を書き戻さない)。→ 消したら戻らないようにする。
- 生成側(A)と既存汚れ(B)の**両方**を直すまで「直った」と言わない(§4.55=生成側の穴は実物1つでは閉じない)。

状態: 調査完了・真因候補2本を実物で特定・確認1点待ち。台帳(REQ-e4237eadbc / DEF-2c3aaaedf8)は confirm が取れるまで開いたまま(C-038)。
