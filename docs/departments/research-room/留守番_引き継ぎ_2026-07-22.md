# 留守番→AD研究室(モドリッチ) 引き継ぎメモ

> 記録: アメス(留守番) / 2026-07-22 / 出典=シャビ・アロンソ(研究室HQ)からの依頼
> ★これは**モドリッチ本人が起きてから判断する事項**。留守番は受領・記録のみで、割り振り/優先順位/実装発注はしていない。

## 1. 体制の訂正(HQからの指令の通し方)
- Chami指摘(2026-07-21):HQ→システム改修部門の流れで、**間の部門長=モドリッチ(AD研究室)が通っていなかった**。
- 今後、**ADAFI事業部への指令はまずモドリッチへ届く**。配下(改修α・フロントエンド・商品候補選定・タイトル文・分析・コンサル情報)への**割り振りと優先順位はモドリッチの判断**。
- 規約:RULES §6.4 に「上から下へ指令を出す時も同じ(飛び級しない)」を追記済。機構でも `scripts/llm/dispatch.py` がHQの配下dept直接投函を止めて部門長を案内するようになった(緊急時のみ `--direct`)。

## 2. β を運用から外した(Chami裁定)
- 理由:「αだけに一方的に情報共有されてβは放置→根底情報が乖離して誤った改修をしてしまう」。
- **改修は α(5secシステム改修部門α)に一本化**。
- Discordの部屋は残す(過去ログ保全のため)。運用から外すだけ=可逆。
- αが塞がった時の並列駆動は当面なし。本格稼働で必要になれば再検討。

## 3. 留守番からの申し送り(未決・要判断)
- 直前まで動いていた案件:**投稿履歴の端末非依存化(スプレッドシートを正本に)**。書き順変更=データモデル変更のため system-engineer 職責、研究室へ回送済(留守番は実装せず、事実確認のみ)。
- X(Twitter)2垢の実装は**優先度を下げる裁定**が出ている(アフィ申請・協業プランの都合で当面投稿不可)。履歴の端末非依存化が最優先。
