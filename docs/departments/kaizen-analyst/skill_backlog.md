# 裏で貯める「作るべきスキル案」(改善提案部門)

> Chami依頼 2026-08-15(便 msg=1537997282610249800)=「それまで裏で作るべきスキル案を探しといて」。
> ★これは**候補の置き場**であって型そのものではない(だから毎朝の新規skill化報告には出さない=
>   `daily_skill_report.py` の INFRA_BASENAMES で除外済)。書き上げて改修αへ渡した時点で
>   `preflight_*.md` として独立し、報告に載る。
> ★数字は測って書く(共通規律§1)。下は 2026-08-15 に `daily_repair_analysis.py --hours 168` で実測。

---

## ★対象の粒(2つの系列・Chami依頼 2026-08-16 で拡張)
- **設計図の粒(骨格)**= 保存経路・履歴同一性のような「どこを1つの権威に束ねるか」の型。例= save-path型。
- **★細粒度(コード面)の粒**= 関数・1行・セレクタの作法。「ここでこう書くと必ず事故る」を検査に落とす。
  Chami原文=「もっと細かいプログラムのとこまでスキル化の対象を広げてよ」(msg=1538456267956428851)。
  → **インシデント.md §3(汎用教訓A〜G)と change_log の"同じ関数/同じ作法での再発"が主な鉱脈**。
  → 検査はビルド無し素JS/CSSなので **grep/小スクリプトの作成時プリフライト**で足りる(CIスモークへ相乗り)。

## 済み(型を書き上げて渡した= 報告に出た分)
- ✅ **番犬(外へ出る待ちの時限)**(細粒度・コード面) `preflight_net-wait-watchdog.md`(裸fetch62箇所中番犬付き3箇所のみ/INC-108/116/125/132/134・8/16書き上げ・Fable棚卸し1位)= 細粒度skillの2本目。
- ✅ **複製判断(同名ヘルパの中身違いコピー)**(細粒度・コード面) `preflight_duplicate-helper.md`(`js/yt-clicks.js` isR2が4定義2変種/INC-112/134/140・8/16書き上げ・Fable棚卸し2位)= 細粒度skillの3本目。
- ✅ **全称スタイルの波及**(細粒度・コード面) `preflight_global-css-scope-bleed.md`(裸`button{width:100%}`が3箇所で後追い修正/INC-37/47 の`[hidden]×flex`・8/16書き上げ)= 細粒度skillの1本目。改修αが `scripts/check_css_global_scope.mjs`(smoke.yml:57)として配線済。
- ✅ **保存経路の単一着地権威**(設計図) `preflight_save-path-single-authority.md`(候補→作成→保存 塊・8/15渡す→c639cae/v=810採用)
- ✅ **数字を装飾面へ載せる**(設計図) `preflight_digit-on-badge.md`(販促ラベル/順位番号・8/14渡す→ae56c2f採用)

## 候補(直近1週の再発クラスタ・大きい順・まだ型が無いもの)

0. 《ピーク実物検査》★NEW(8/16巡回)= **「直した」を実データで検証する運用時検査**。ピーク記録は
   8/2・8/6・8/11 に3回改修しても 8/15 に「5回言ってる」で🔥(Z2の典型)。ソース文字列一致でなく、
   **GAS記録シートのピーク列が改修後N時間で実際に非空になるか**を実物で assert する型。→ アスナが起票。

1. 《UI幅》→ ★**起票不要**(Fable棚卸し 2026-08-16 D)。改修αが既にCI門3本を配線済=
   `check_ui_ios.mjs`(smoke.yml:44)/`check_button_width.mjs`(:50)/`check_css_global_scope.mjs`(:57)。
   UI幅系はこの3本で実質カバー済。残るグローバル`button{width:100%}`の既定反転は skill でなく
   **C-039 のChami承認案件**(check_button_width.mjs 自身が「静的には拾えない」と自書き)。

2. 《投稿導線》**投稿履歴・投稿導線の一意性**= 週38件で最多。ただし分類器の語「保存中」が
   このクラスタへにじむ(PDCA 8/15 §6)ので**真値は精査要**。芯は「同じ投稿が2行に割れる/
   投稿日時が収束しない/短縮URL空欄」= **投稿1件の同一性を1つの鍵で束ねる**型が要る見込み。
   → 次の精査便で真値を出してから起票(盛らない)。

2. 《UI幅》**ボタン/レイアウト幅の作成時プリフライト**= 週10件。半角括弧・1080×1920座標・
   紫禁止と同じ「作る時に機械で弾く」系。`frontend/design-preferences.md` を検査に落とせるか。

3. 《同期》**端末間同期/ドラフトの単一正本**= 週10件。保存経路型の**隣**(遷移後の同期)。
   save-path型が効いてから、残る差分だけを型化(先に出すと二重になる)。

## 候補(細粒度・コード面・8/16巡回とインシデント.md/Fable棚卸しから採掘)
- ★次に書く順(Fable棚卸し 2026-08-16)= 番犬✅→複製判断✅ の次は **D世代トークン(4位)→ C時間予算(5位)**。
- D. 《世代トークン》★NEW(Fable 4位)= 共有状態(前景画像・一覧DOM・録画blob)へ書く非同期継続は、**世代番号を照合してから書く**。古い応答が新しい状態を上書きする穴。同じ解法が**5回別々に再実装**(INC-07/15/135/139・eee46dc)=型が凍っていない典型。検査は grep 一発不可(onload代入は正当例多数)=**世代管理必須の関数の登録簿**を持ち `_gen`/世代照合の存在をassert。書き上げは番犬/複製判断より1段重い。
- C. 《硬い制限下の重い定期処理は時間予算で頭打ち》(INC-116 教訓2/3・Fable 5位)= GAS6分・cron間隔のような硬い制限へ重い処理を足す時は**時間予算とセット**。完走しない処理は「たまに失敗」でなく「全滅→自動停止」になる(INC-116=snapshotStatsが全履歴バックフィルで6分超→Google自動停止→ピーク全滅)。検査=トリガー配下関数へ `Date.now()-t0` 系の頭打ちが在ることをassert+新規ループ追加commitで警告。証拠INCは1本のため頻度根拠では4位までに劣後。
- B. 《採用値でイベントを渡す》(INC-04)= 保存/記録は**入力の候補**(selectedPostFile)でなく**処理が最終採用した値**(pcSelectedFile‖元写真)をペイロードに載せる。→ **保留維持**(Fable D)= save-path型の効果確認(8/22測り直し)より先に出すと二重。
- A. 《手前で殺す二段障害》(INC-116)= 1つ直して同じ症状が再発したら「今の症状の直接原因」を実行結果で切り分ける。→ **skill化保留**(Fable D)= これは作成時プリフライトでなく**障害調査の手順**。インシデント.md §3 への1行追記(調査規律)で足り、preflightにすると空箱になる。

## 候補(運営・基盤・研究室層＝Chami依頼 2026-08-16 msg=1538577522147459272 で拡張)

> Chami原文=「運営全般的にスキル化できるところはお願い。研究室HQとか頻繁に他の研究室とかとChamiなしで
>   なんかやり取りしてるし」。→ これまでの型は全て**改修α(5秒動画本体)**に寄っていた。基盤・常駐・
>   研究室間コーディネーションは**私の測定エンジンの外**だった(`daily_repair_analysis.py` は
>   `dept=system-engineer` のみ走査)。ここを開ける。
> ★実測(2026-08-17・`change_log.jsonl` 963行を dept 分類)= 改修α **421行(44%)** に対し、基盤/研究室層
>   (aegis-gl 128 / hq 91 / platform-se 23 = **242行/25%**)が第2の鉱脈。私の型づくりが未着手の面。
> ★**この面の型の渡し先は改修αではない**(C-015)= 基盤・常駐 → **プラットフォームSE / イージス研究室**、
>   組織の裁定・規律 → **研究室HQ / 人事部門**。私は型を書き、そこへ渡す。実装はしない。

基盤/研究室層 242行のキーワード頻度(実測・大きい順・emoji除去後):

O1. 《常駐ライフサイクル》★最有力= keeper 20 / 常駐 18 / 復活 16 / 載せ替え 12 = **同系66件**。
    「常駐を退役させたら監視も降ろす(C-044)」「読むものを足したら載せ替え経路も決める(C-042)」が
    **裁定として繰り返し出ている**＝型が凍っていない証拠。作成時プリフライト＝「常駐を足す/退役させる/
    読むものを変える時のチェックリスト(supervisor codever と keeper/DEPTS の2機構どちらを触るか・
    センサーの読み手を全数数えたか・復活タスクは本番条件を1回通したか)」。渡し先=**プラットフォームSE/
    イージス研究室**。★根拠INC=[[daemon-reload-two-mechanisms]]/[[never-fired-never-verified]]/C-042/C-044。

O2. 《報告の押し出し》= 報告 27 / pulse 10 / 押し出し 5 = **42件**。「実体が動いてもChami名義で
    押し出さないと"応答なし"に見える」(§4規律)が繰り返し再実装。`report_pulse` の部屋別自動押し出しは
    8/11に停止済＝**各部門が自分で押し出す型**が要る(現状は口伝)。渡し先=**研究室HQ**(共通規律の執行形)。

O3. 《研究室間デリバリ》= queue 13 / 回送 12 / 箱 8 / inbox 5 / dispatch = 配達の穴が周期的に再発
    ([[drain-window-loses-messages]]/[[main-box-dept-duplicates]]/[[escalate-needs-live-consumer]])。
    芯=「回送フラグは箱に積むだけ・生きた消費者が要る」。型=**dispatch/escalate の前に消費者の生死を
    presence --check で測る作成時ガード**。渡し先=**プラットフォームSE/イージス研究室**。

O4. 《裁定・規律の機構化》= 裁定 42 / 規律 29 = 最多。ただし**これは既にHQが回している skill化そのもの**
    (Chami指示→共通規律/裁定カタログへ1行=編集で配布完了)。私が二重に型を作る面ではない=**除外**。
    §3.9(横展開)で私が種を送る側に回る所。新規の型は起こさない。

★次の一手(この面)= O1《常駐ライフサイクル》の作成時プリフライトを1本書き上げて
  プラットフォームSE/イージス研究室へ渡す(改修α向け preflight と同じ様式)。O2/O3 はその後。

## 数え方の但し書き
- 上のクラスタ数は改修α(system-engineer)の change_log 実測=**再依頼そのものではない**(Z1とは別軸)。
  Z1(Chamiが同じことを何回言ったか)の横断カウントは別途。ここは「改修が集中している所」の代理。
- 「投稿履歴」の語のにじみは道具の穴(過去の柱を動かすため即修正しない・別便で提案)。
