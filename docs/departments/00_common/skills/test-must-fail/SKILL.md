---
name: test-must-fail
description: テストや検査を新しく足した時・写像(マッピング)や判定を変えた時に使う手順。tests/ 配下や docs/departments/qa-reviewer/checks/ に検査を追加したら必ず通す。「検査を足した」「テストを書いた」「回帰確認して」で使う。常にPASSする検査(空PASS)は無いのと同じ、を機構で止める。
---

# 検査は「落ちること」を1回見てから置く

## なぜ要るのか
改修ログ736行のうち **61件**が「検査を足したが落ちない/写像を変えたのにテストが旧前提」の型だった。
**常にPASSする検査は、検査していないのと同じ**で、しかも「守られている」という誤った安心を配る。
これは沈黙の事故=最悪の形だ。

## 手順(順番を変えない)

1. **★0歩目= 壊れている側を1回見る。** 検査を書く前に、いま実際に壊れている実物(または
   わざと壊した入力)を用意する。後の比較対象がこれになる。
2. **検査を書く。**
3. **★落ちることを見る。** わざと壊した状態で走らせ、**FAILの文字を目で見る**。
   FAILしないなら検査が間違っている=実装ではなく**検査の妥当性を先に疑う**。
   よくある空PASSの原因= 対象ファイルが見つからず0件で回った / try-except が握り潰した /
   条件が常に真 / 実行はされたが assert が無い。
4. **直す(または元に戻す)。**
5. **PASSすることを見る。** 落ちる→直る の**両方**を見て初めて、その検査は生きている。

## ★「落ちるのを見た」でも足りない型がある(2026-08-18 研究室HQ・実害つき)

上の手順3を通っても抜ける穴が1つある。**ソースの文字列一致で「引数を渡したか」を見る検査**だ。

    chk("Xがtagを渡す", 'tag="room"' in src)   # ← わざと消せばちゃんとFAILする=手順3は通る

だが**通るのは「その文字列が在るか」だけ**で、口が実際に効くかは見ていない。
実例(2026-08-18)= `gemini_responder.py` に `ask(content, tag="room")` は在り検査は緑。
だが**それはホイミンの行で、ベホップの経路は同じファイルの別の行**が既定のまま漏れていた。
**緑の検査の下で穴が生きていた。**「未検証の枝」より悪い——誤った緑は探しに行かせない。

- ★**思いつく壊し方しか試せない。**文字列一致の変異検査は「文字列を消す」しか壊せず、
  **別の呼び出し口**という壊れ方は最初から視野に無い。
- ★**だから引数の受け渡しは実行で見る**= 外へ出る手(HTTP・送信・起動・書き込み)だけ偽物にし、
  **判定と分岐は本物のまま**その関数を呼び、**実際に渡った値**を集めて突き合わせる。
- ★**跨ぐ経路は跨がせて見る。**キーの切り替え・フォールバック・降格は、
  **切り替わった瞬間にしか出ない取り違え**がある(実例= 429で私用キーへ跨いだ行が
  事業用として記録されていた)。1本目でわざと失敗を返し、2本目まで走らせること。

## 走らせ方(このプロジェクト)
- JS= `node tests/test_xxx.js`(例 `tests/test_idb_failopen.js`)
- Python= `python tests/test_xxx.py` / 常駐まわりは `python scripts/_daemons/test_daemon_keeper.py`
- 部門横断の一括点検= `python docs/departments/qa-reviewer/checks/run_all.py`

## 写像・判定を変えた時の追加規則
- ★**その写像を読むテストまで一緒に動かす。** 片側だけ直すと、テストは旧前提のままPASSし続ける。
- ★**共有される関数に条件を足したら、呼び出し元を全部数える。**
  「この経路は別なので影響なし」と書いたなら、**その主張こそ数え直す**。
- 既にある検査ツールを使う(`bump.mjs --check` / `registry_tool.py --check` / `status.ps1`)。
  **その場のワンライナーで数え直さない**——毎回違う嘘をつく。

## 報告の言い方
- 「検査を足した」だけなら **入れた(確認待ち)**。
- 「落ちるのを見た/直ってPASSした」まで行って **効いた**。
- **症状が出ていたのと同じ場面**の実物を見るまで **直った** と書かない(§4.55)。
