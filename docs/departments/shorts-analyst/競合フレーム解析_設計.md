# 競合Shorts フレーム/ビジョン解析 — 着手可否＋設計 (shorts-analyst 主管)

> 主管確定: 2026-07-30 HQ裁定(msg DISPATCH-shorts-analyst-1785342280732)。分析部門(三笘薫 主管＋アーモンドアイ)＋ベホップ(Gemini vision・C-017)。
> 元はプラットフォームSEに預けていたが、charter不一致(画像解析は範疇外)で外れた。
> 実名・videoIdは公開repo禁止。この docs は local(非公開)なので実名記録可(裁定C-013)。

## 結論: 着手可否 = **成立する**(実弾1本で確認済み・2026-07-30 01:26 JST)

競合Short 1本で 収集→フレーム抽出→vision解析 の全経路を通し、**焼き込みフック文字とコマ内容の両方を正しく取れた**。

### 実測(1本・アニメ同人フェス @anime_doujin_fes / videoId=z-hIYiVrSro)
- **収集**: `yt-dlp -f "bv*[height<=480]+ba/b[height<=480]/b"` → webm **145KB**(Shortは軽い)。
- **尺**: `ffprobe` = **5.041秒**。
- **抽出**: `ffmpeg -ss 4.5 -i vid.webm -frames:v 1 -q:v 2 frame.jpg` → **32KB** jpg(4.5秒目・Chami指定位置)。
- **vision**: `scripts/behop/behop.py --image frame.jpg --ask "<JSON指示>"` → 下記を返した(この瞬間 gemini-2.5-pro が割当超過→gemini-flash-latest へ自動降格。**flashでも全文読めた**):
  - `frameText`: 「引用作者: 左藤空気 アイドルスキャンダルの末路 アイドルに戻る 本当にありがとうございました！ プロデューサーさん お疲れさま」← タイトル・作者名に加え**コマ内の細かいセリフまで**拾えた。
  - `panelDesc`: 「アイドル衣装の黒髪と茶髪の女性2人…ステージ登場のポーズ、汗を浮かべた表情のアップ、プロデューサーに挨拶する場面」← 目視と一致。
- 目視でも確認(フレーム画像を人が見た): タイトル焼き込み「アイドルスキャンダルの末路」・引用作者表記・左列モノクロ漫画コマ／右列カラー立ち絵のコラージュ=競合の典型フォーマット。

### 分かった注意点(バッチ化の前に効く)
- **gemini-2.5-pro は割当上限に当たる瞬間がある**。15ch×多数本のバッチは **flash基準**で設計するか、pro使用時はレート制御が要る。品質は flash で足りている(コスパ良)。
- yt-dlp が JavaScript runtime 警告を出す(deno等が無い)。**今回は取得できた**が、将来 format 欠落の可能性=収集足回り側で runtime を入れる判断はサブ発注先に委ねる。

## 設計: 収集→抽出→vision→勝ちパターン

| 段 | 中身 | 担当 |
|---|---|---|
| ① 収集足回り | 監視chの各ShortのvideoId(βGAS comp_titles が既に保持)→ yt-dlp で最小画質DL → ffmpeg で **4.5秒目**を1枚jpg抽出 → 所定の場所へ置く。PC側バッチ。 | **サブ発注**=改修α(system-engineer・PC側スクレイパの畑)。丸ごと基盤に置かない(HQ裁定)。 |
| ② vision解析 | フレームjpg → ベホップ(Gemini vision)で `{frameText, panelDesc}` を抽出。videoIdをキーに格納。 | **分析部門(自室)＋ベホップ** |
| ③ 格納 | frameText・panelDesc を **videoIdで comp_titles と突合**できる形で保存(βGASシートに列追加 or 別シート。既存の題名/速度/投稿時刻と結合)。 | 分析部門＋②の足回り |
| ④ 勝ちパターン抽出 | 焼き込みフック文言の型・コマ構成の型を、速度TOP/クリック実績と突き合わせて分析。copy-directorへ渡す実弾にする。 | **分析部門(自室)** |

### サブ発注の切り出し(①のみ・最小)
①だけを改修αへ出す。②③④は自室で回す。①の受け入れ条件=「監視chの各Shortについて、4.5秒目の32KB前後のjpgを videoId 紐付けで所定の場所に置く」。頻度は既存の競合βGAS収集に相乗り(週次で足りる想定・要調整)。

## 次の一手
- HQへ着手可否=成立を返す(この設計書パスを添えて)。
- ①の足回りサブ発注は、HQ承認後に改修α(system-engineer)へ dispatch(実装Go≠設計承認=C-001。まずHQ裁定を待つ)。
- ②以降は自室で先行して回せる(手動でも frameText/panelDesc は取れることを実証済み)。

## 実装状況 2026-07-30(設計完了・自室で全ループ稼働)

- HQ再裁定(msg 1785342886539)= Chami直接指示で**①収集足回りも自室**(改修αは5S改修中で回さない)。①②③④すべて分析部門で完結。
- **バッチは既存だった**= `scripts/comp_frames.py`(改修α 2026-07-29作)＋GAS側 `comp_frame_pending`(コード.gs:483)/`comp_frame_write`(:840)＋`behop.ask_pro`(quota処理付き)。フルスタック実装済み。★rebuildせず検証した(§3.8)。
- **model方針を flash基準へ修正**(commit時)= 旧スクリプトは pro専用(07-29の条件付きChami発言が根拠)。**実測でflashが全文読めた=条件不成立**＋HQ最新指示「flash基準で安く」。`BASE_MODEL=gemini-flash-latest`へ。pro無料枠は実測で即quota=バッチ不適。
- **実機で全ループ確認**(2026-07-30):
  - `comp_frames.py --check` → pending 10件(実データ)。
  - `--dry --limit 1` → 実pending eXZqfTcv7rM の4.5秒目から flash が `frameText='告白を断った理由 桐島さん、好きです！付き合って'` / `panelDesc='セーラー服にカーディガンを羽織った金髪の女子生徒が…'` を videoId紐付けで取得。
  - `--limit 1`(本番) → 同videoIdを **GASシートへ書き戻し1件成功**(frameText=作者様：あくめくん/告白を断った理由/…、panelDesc=…)。①収集→②vision→③格納のループが閉じた。
- 残り= 通常運用でpendingを消化(pro枠に頼らずflashで安く)＋④勝ちパターン抽出(焼き込みフック文言の型・コマ構成の型を速度TOP/クリック実績と突合)。④は自室の分析本務。

### テスト残置物
`local/_work/frametest/`(vid.webm・frame_4p5.jpg・各ログ)。videoId=z-hIYiVrSro はこの docs(非公開)にのみ記載、公開repoには出さない。
