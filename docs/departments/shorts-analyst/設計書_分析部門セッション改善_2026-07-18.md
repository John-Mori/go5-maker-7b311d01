# 設計書: 分析部門(shorts-analyst)セッション改善 (2026-07-18)

> 発端: Chami依頼(2026-07-18 chatペイン)「このセッションの目標をより高品質に達成し続けるための改善、欠点、考慮が足りていない点の設計書を。現状あまり使っていないけど」。
> 本書は**提案**であり、実装は承認後(改善は承認制=orchestration.md)。作成には多角調査ワークフロー(事実収集5+提案3レンズ+批評1=9エージェント・全完走・素材の重複17件を本書で7件へ統合)を使用。

---

## 0. 結論(先にすべて)

- **土台(待機・脈・返信)は健全だが、配達チェーンは一度も実発火しておらず、業務を支える運用資産はほぼゼロ。** 仮説台帳(hypotheses.md)・実験記録(experiments.md)は2026-07-11設置後「(まだなし)」のまま。BOOT.md・STATUS.md・データ取得手順書が全て不在(BOOT.mdは他10部門に有り当部門だけ無い)。
- **「使われていない」の根本原因3つ**: ①頼み方と返る様式がChamiから見えない ②数字を引く経路が口伝(セッション毎に再発見・実際に唯一の知見が「初回の見立て誤り→Chami訂正」の経緯) ③受け身専業で、数字を見に行く起動トリガーが存在しない(kaizen設計書が特定した「待つ設計と能動ミッションの矛盾」と同型)。
- **調査中の重大発見(分析所掌外・即日エスカレ)**: gas/コード.gs:174 の `action=delete`(記録行の削除)が**無認証**でJSONP面に露出(現物確認済)。分析の利便より先に是正が要る。
- **最初の裁定=部門の存続形態(裁定1)**。本書の全改善は「常駐維持」が採択された場合にのみ意味を持つ。

---

## 1. 現状把握(このセッションの実測)

- 2026-07-17 01:50頃開窓以降、**受信0件・45分TTL空起床7回・実分析0件**。waiter脈(`local/llm/claude_active_shorts-analyst.txt`)の更新は実測確認済=配達条件は満たしている。persona_send送信は4回全てHTTP 204。ただし「ch発言→poller→部門箱→waiter鳴動→既読✅→着手👀→processed記帳」の完全チェーンは**未発火**(=発火しない安全網は検証されない)。
- 資産の実態: hypotheses.md/experiments.md=空。metric-definitions.md=実質1行。知見.md=1件のみ。handoff_イベント駆動ウェイク検討.md=中身は実装済(inbox_waiter)なのに完了マーク無しでopen状態に見える。persona_detail.md(948行)は視聴維持率・スワイプ率・CVR等、**agent定義が観測不可と確定した指標**を担当領域として大量記載(2026-07-12から乖離したまま)。
- データ経路: 4本が疎通実測済(2026-07-18ワークフロー調査)。①GAS Web App GET(deltas/history/stats_tail/comp_digest/comp_titles・exec URL正本はqa-reviewer STATUS.md §3-2) ②D1直SQL(`npx wrangler d1 execute go5_kaizen|go5_fanza --remote --json --config fanza-worker/wrangler.toml`・works.sales_n=市場販売数) ③link-worker `/api/stats`(短縮URLクリック実数) ④Bluesky公開API getPosts(未認証・25件/回)。**全て口伝状態で部門docに無い。**
- 取れない数字(現状): 記録シート全行(いいね・カテゴリ列込み)/視聴履歴564行の全量(stats_tailはn≤20固定)/verify_fanza・sch_state_v1(端末localStorage閉じ込め)/任意videoIdの即時再生数(GAS間接値のみ)。成約数・視聴維持率は**観測不可が確定事項**(.claude/agents/shorts-analyst.md)。

---

## 2. 欠点・考慮不足の一覧(優先度つき)

| # | 領域 | 問題 | 対策 | 承認 |
|---|---|---|---|---|
| P0-1 | 安全 | gas/コード.gs:174 `action=delete`が無認証露出(URLを知れば誰でも記録行を消せる。exec URLは部門docに記載あり=秘匿バリアと見なせない)。:178 `snapshot_now`も無認証(軽微) | system-engineerへ是正依頼→§3.1 | 依頼のみ=不要 |
| P0-2 | 起動 | BOOT.md不在。起床の正順(mv退避→即再武装→既読→着手→記帳)・退避先`local/_work/`固定が成文化されておらず、INC-85/86/100/103の罠を毎回踏み得る。分析は90秒超の没頭型作業が多く箱剥がしの主因条件に最も合致 | BOOT.md新設→§3.2 | 不要(自部門doc) |
| P0-3 | 採用 | 頼み方と返る様式が見えない=依頼ゼロの直接原因。観測不可の線引きもChamiから見えず、頼めないものを頼んで失望する事故も起き得る | menu.md+掲示→§3.3 | 掲示1回 |
| P1-4 | データ | 4経路が口伝。罠5点(JSONP剥がし/deltasはchannel混在=acc1-/acc2-接頭辞で自力分離/stats_tail n≤20/history postUri空行/wranglerは家PC認証依存)を知らないと数字を誤読する | data-paths.md→§3.4 | 不要 |
| P1-5 | 蓄積 | 台帳ゼロ・指標辞書なし。唯一の実仮説(投稿時間カレンダー)は知見.mdに番号も期限も無く埋没、しかも検証条件「30m初速」はスナップショットが毎時のため**観測不可能=検証設計が破綻** | 台帳運用+指標辞書→§3.5 | 不要(裁定3のみ) |
| P1-6 | 能動 | 数字を見に行くトリガーが無い。数字は今すぐ引けるのに誰も見ていない | 週次数字便→§3.6 | 裁定2 |
| P2-7 | 衛生 | handoffがopen状態(二重起票リスク)/persona_detailと観測範囲の乖離/報告様式なし(■レポート体退行の既知傾向と最悪の相性) | 衛生バッチ→§3.7 | 不要 |
| P2-8 | 検証 | 配達チェーン・react.py・部門名義送信の一部が未発火 | 初回実務相乗り検証→§3.8 | 不要 |
| P2-9 | 費用 | 45分TTLの空起床(実測7回/1.5日)。ただしTTL値は組織横断論点(product-scout裁定1と同件・イベント駆動本改修はsystem-engineerへ引き継ぎ済) | 本書では裁かず組織裁定に従う | 組織裁定 |

---

## 3. 改善設計

### 3.1 P0-1 GAS無認証deleteの是正依頼(即日・依頼のみ)

- 内容: `action=delete`(deleteRecord_)へ`shortSecret_()`同等のソフト鍵照合を追加、`snapshot_now`も同様(いずれもフロントの正規利用と互換を保つ形はengineer設計)。分析部門は**所掌外のため依頼のみ**・自力修正しない。
- 受け入れ条件(依頼側): 改修-依頼chへ行番号・現状・推奨対策つきの依頼が送信され(HTTP 204)、本書§2表にリンクされること。是正完了の確認はqa-reviewer所掌。
- ★分析部門の全データ系改善(§3.4の将来拡張含む)より**先**。読み出しactionの追加提案は是正完了まで凍結。

### 3.2 P0-2 BOOT.md新設(起床の正本)

BOOT_TEMPLATE.md準拠+部門固有。必須記載(全て実パス・実コマンド名指し=INC-86教訓「曖昧な指示は必ず踏まれる」):
1. cwd自己点検(go5-maker直下でなければ作業せずChamiへ開き直し要請)
2. チャイム線=`python scripts/llm/inbox_waiter.py --name shorts-analyst`を**ハーネス管理のバックグラウンド**で起動(シェルの`&`禁止・TTLは有限のまま=INC-091[2026-07-14セッション暴走・台帳INC-91とは別件]の不変条件)
3. **起床の正順**: ①`mv local/inbox/shorts-analyst.jsonl local/_work/shorts-analyst.jsonl`(inbox/内退避は絶対禁止=sweepに食われる) ②即waiter再武装 ③読んだ直後に`react.py --emoji 既読` ④作業開始時に`--emoji 着手`→処理→msg_id単位で`local/discord_processed.jsonl`(正はこちら)へ記帳→work削除
4. 600秒超の長時間分析中は`inbox_waiter.py --name shorts-analyst --once`で脈を打つ(箱剥がし回避)
5. INC-98: どんな理由で始まったターンでも、終える前にwaiter生存確認
6. 報告は**短文でも**`persona_send.py --body-file`(数字・URL・コード表記はバッククォート/$()沈黙消失の罠に最も該当)・HTTP 204確認まで「送った」と言わない・出典リストはDiscord表面に書かず台帳へ
7. 人格=モドリッチ/アーモンドアイ。喋る前にmanifest確認(■レポート体退行の既知対策)・冒頭で名乗らない・アーモンドアイは男性一律さん付け・Chami呼称(モドリッチ=Chami/アーモンドアイ=ちゃみ)
8. モデル規定: 常駐セッション=研究室指定に従う。委譲は数字取得=haiku/sonnet・多変量解釈や月次傾向=opus格上げ(orchestration表準拠)。規約文書に触る編集はsonnet以下へ委任しない
9. dept_tasks掃引(post.published/metrics.updated/competitor.weekly_digest)は**イベント生産者の稼働を確認してから**有効化(確認前に毎起床掃引を書くと、永遠に空の表を掃く儀式+安いTTLターンの肥大化になる)

受け入れ条件: ①曖昧語ゼロで7項目以上が実コマンド付きで載る ②初回実起床が正順どおり実行され証跡(既読msg_id・記帳行・waiter生存)が残る ③第三者がBOOT.mdだけで手順を再現できる(再質問ゼロ)。

### 3.3 P0-3 menu.md(頼み方の常設・唯一の「使われ始める」レバー)

番号付きメニューを定義し、承認後に分析部門chへ1回掲示(以後「分析2やって」の一言で発注可能):
1. 今日の動き(deltasサマリ・結論2文+上位3変動・所要目安5分)
2. 投稿別成績(history+クリック実数+Blueskyエンゲージメント・表形式・10分)
3. 競合週報(comp_digest/comp_titles読解・10分)
4. 仮説の現在地(台帳読み上げ・3分)
5. 自由分析(お題指定・所要は受注時に回答)

各項目に「返る様式の見本」と「**受けられないもの**(成約数・視聴維持率・スワイプ率=観測不可)」を明記。所要目安は実測前の暫定と明記。
受け入れ条件: 掲示がHTTP 204/番号のみの初回発注を追加質問ゼロで受理し目安内に納品/メニューに観測不可指標が載っていない。

### 3.4 P1-4 data-paths.md(データ取得手順書・1本のみ)

- 実測済み4経路のコマンド実例+期待レスポンス形+[実測済み]ラベル+実測日。JSONP剥がしのワンライナー例。罠5点の明記。「取れない数字」一覧と回避策・依頼先。正本ポインタ(gas/コード.gs doGet:111-188・exec URL正本はqa-reviewer STATUS.md)。
- **secret実値は書かない**(参照先の関数名のみ・レポートへの書き出し禁止も明記。grepで機械確認)。
- 後段(凍結): 取得スクリプト化(scripts/analytics/fetch_metrics.py)とGAS読み出しaction追加(records/stats_range)は、**§3.1の是正完了+実依頼が複数回発生してから**、仕様を確定してsystem-engineerへdept_tasksで依頼(いま基盤投資をしない=需要ゼロの部門にP0でスクリプト基盤は過剰、という批評を採用)。

### 3.5 P1-5 台帳の実運用開始+指標辞書

- hypotheses.mdへ初エントリ**SA-H001**(投稿時間カレンダー)を登録。様式=6項目(id/結論1行/根拠n/状態[提案→検証中→採択/棄却→置換]/検証条件/期限)。30m粒度問題は裁定3で明示的に裁く(黙って放置しない)。
- STATUS.md新設: open案件台帳(初期1件=「30m/1h初速データ収集経路の有無をsystem-engineerへ確認」・待ち先・期限つき)。未クローズ案件をセッションの記憶に置かない(INC-96同型対策)。
- metric-definitions.mdを指標辞書化: deltasフィールド辞書(tv/yv/wv・tc/yc/wc・twc/ywc/wwc・peaks)/シート列定義(投稿日時列=YouTube実投稿時刻・Chami明示2026-07-13を転記)/ID規約(acc1-/acc2-接頭辞・code=shortUrl末尾・postUri空=YouTube主体で正常)/観測可能・不可能の線引き転記。意味未確定の列は「未定義」と正直に書く。
- 受け入れ条件: 「(まだなし)」が消える/新セッションが台帳だけで仮説の現在地を1ターンで答えられる/シート列の誤読が再発した場合それが「既知の未定義」であること。

### 3.6 P1-6 週次数字便(裁定2で承認されたら開始)

- 方式: kaizen-analyst先例のマーカーファイル方式を流用(**コード変更ゼロ・新規cron/常駐なし・TTL45分不変**)。WAITER:TTL満了起床のターン内で`local/llm/shorts_analyst_last_weekly.txt`を確認→7日超過時のみ実行→touch。7日未満は従来どおり再武装のみ(待機中は非表示の規律維持)。
- 内容: deltas+history+エンゲージメント+go5_fanza販売数→「観測(n必須)→仮説→次のアクション」20行以内→モドリッチ名義`--body-file`で分析部門chへ→台帳を最低1件更新→改善insightはkaizen-analyst chへミラー(Chami直行禁止ルート遵守)。
- **撤退基準(自己防衛)**: 2便連続でChamiの反応(発注・リアクション・言及)ゼロなら、3便目の代わりに「停止か形態変更か」を裁定として自ら提示する。使われない改善を作り続けない。
- 限界(正直に): セッションが閉じている週は便が出ない(mainフェイルオーバーは箱回収のみ)。確実な週次が必要なら常設ジョブ化=別裁定。
- 受け入れ条件: 初回便がHTTP 204・20行以内・acc1-/acc2-混在なし・n付き/7日未満のTTL起床で何も送信されない実測1回/便ごとに台帳差分1件以上。

### 3.7 P2-7 衛生バッチ(半日・全て自部門doc内)

- handoff_イベント駆動ウェイク検討.md冒頭へクローズ行追記(「実装済=scripts/llm/inbox_waiter.py・本書は歴史文書」)+handoffライフサイクル規約1行をBOOT.mdへ(クローズ行の無いhandoffのみが生きた引き継ぎ)。
- persona_detail.md冒頭へ注記1節のみ追加(観測不可指標の確定事項と旧AFI語彙対応表。948行の本文は無改変=安価・ロールバック容易。全面改稿はChami裁定要のため保留と明記)。
- 報告様式.md新設: 結論2文先頭→観測(n・接頭辞必須)→仮説→次アクション・20行以内・確度ラベル([実測]/[机上])・出典は台帳参照で表面に書かない・`--etitle`(embed)は3900字で黙って切れるため長文に使わない。
- inbox_waiter.py:35のdocstring旧値(「90秒」・実際はRESIDENT_FRESH_SEC=600秒)は、dept_tasks起票せずsystem-engineerの次回接触時に相乗り指摘(コメント1行に組織プロセスは過剰、という批評を採用)。

### 3.8 P2-8 初回実務相乗り検証(専用テストは立てない)

- Chamiにテスト発言を頼む専用儀式は**やらない**(二度入力させない原則・scout P2-10方式を採用)。最初の本物の依頼をそのまま検証機会にし、①部門箱への実配達 ②鳴動レイテンシ ③react.py既読/着手の絵文字解決 ④正順の完走 を[実測済み]ラベル+msg_id+時刻で知見.mdへ1行記録する。不発の段はその場で止め、原因つきで記録(コード起因はsystem-engineerへ)。

---

## 4. 裁定が必要な事項(Discordで番号つきで確認する)

1. **部門の存続形態**: (A)常駐維持+本書の採用レバー(menu/週次便)+撤退基準つき ★推奨(新体制「部門宛てはまず部門セッション」2026-07-17と整合・待機コストは純Python=ほぼゼロでTTL空起床のみ) (B)常駐廃止→研究室からのオンデマンドsubagent呼び出しへ縮退+数字はkaizen週次便へ相乗り(最小コスト・agent定義は既にsubagentとして実用的) (C)現状維持(常駐だけ続け改善なし=推奨しない)
2. **週次数字便**: (A)独立便で開始(撤退基準つき)★推奨 (B)kaizen週次便へ相乗り(Chamiの読む場所を1つに保つ) (C)やらない
3. **SA-H001(投稿時間カレンダー)の検証条件**: (A)毎時粒度で再設計(観測可能な形に落とす)★推奨 (B)30m粒度の観測手段の実装をsystem-engineerへ依頼(コスト発生) (C)棄却
4. (参考・本書では裁かない)TTL帯別延長はproduct-scout裁定1と同件の組織横断論点。組織裁定に従う。

---

## 5. 実施順(承認後)

| 段階 | 内容 |
|---|---|
| 即日(依頼のみ・承認不要) | §3.1 GAS無認証delete是正のエスカレーション(改修-依頼chへ) |
| 即日(承認後・半日・コード変更ゼロ) | BOOT.md→menu.md掲示→報告様式.md→SA-H001+STATUS.md→衛生バッチ |
| 初回実務時 | §3.8 配達チェーン・進捗印の相乗り検証→知見.mdへ記録 |
| 裁定2承認後 | 週次数字便の初回実行(本番TTL起床で1回通す) |
| 需要実証後(実依頼が複数回) | fetch_metrics.pyスクリプト化・GAS読み出しaction追加の仕様確定→system-engineerへ依頼(§3.1是正完了が前提) |

---

## 6. 本書自体の限界(正直に)

- ワークフロー素材(3レンズ17提案)はID衝突・受け入れ条件の相互矛盾(TTL延長vs45分前提)を含んでいた。批評エージェントが検出し、本書で7件に統合・矛盾は「TTL45分維持で統一」側に倒して解消した。
- 所要目安・様式の行数上限は実依頼ゼロ時点の暫定値。初回実務で較正する。
- 裁定1で(B)縮退が採択された場合、§3.2/3.3/3.6は縮退版(subagent呼び出し手順書)に置き換わる=本書の過半が失効する。それでも§3.1(セキュリティ)・§3.5(台帳)・§3.7(衛生)は形態に依らず有効。
- GAS delete露出の悪用実績の有無は未調査(実行ログの確認はsystem-engineer所掌)。
- 配達チェーン未発火のため、§1の「土台は健全」は脈・送信の部分実測に基づく推定を含む(完全チェーンの証明は§3.8で行う)。

## 7. 本書の受け入れ条件

- Chamiが裁定1〜3を番号だけで返答できる(選択肢が自己完結している)。
- 承認された項目が§5の順で着手され、各受け入れ条件の証跡(HTTP 204ログ・msg_id・ファイル差分)が残る。
- 2週間後に「依頼数/週」「便への反応」を計測し、ゼロが続けば§3.6の撤退基準が実際に発動される(発動しなければ本書自体が「使われない改善」の再生産=失敗と判定する)。
