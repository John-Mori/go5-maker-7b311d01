# handoff: P0残タスク(PC実測が必要な3件) 2026-07-16

- 発行: 監査・復旧補佐セッション(クラウド・Discord/D1到達不可のためPC側へ委譲)
- 根拠: `docs/設計・調査/改善設計書_復旧・AI補佐体制_品質継続_2026-07-16.md` §2(Chami承認「進めて」2026-07-16)
- 宛先: PC側の研究室セッション(実装はsystem-engineer規約に従う)
- ★完了したら本ファイルの各タスクに結果を追記し、D1 `system_changes` へ記録すること(クラウド発の記録代行=設計書P1-6)

---

## タスク1 (P0-1): inbox_waiter.py の実体確認と決着

**問題**: 全BOOT文書(orchestration.md:176/BOOT_TEMPLATE.md/BOOT_main.md/research-room/BOOT.md/dept_boot_prompt.py)が起動必須と指示する `scripts/llm/inbox_waiter.py` が**リポジトリに存在しない**(全ブランチ・全履歴で確認済み)。PC上に未コミットで実在する可能性が高い(インシデント.md INC-85/86/94に実測記述あり)。

**手順**:
1. `dir D:\SougouStartFolder\go5-maker\scripts\llm\inbox_waiter.py` で実体確認
2. **実在する場合**: 内容を確認し(秘密が入っていないこと)、コミット+push。orchestration.md:176の「旧heartbeat.pyは互換シムで転送される」の記述が実態と合うかも確認し、合わなければ記述を実態へ修正
3. **実在しない場合**: orchestration.md/BOOT全文書/dept_boot_prompt.pyの該当記述を「`heartbeat.py --name <dept>` を使う」へ訂正

**受け入れ条件**: リポジトリを新規cloneした環境で、BOOT手順の1手順目(脈打ち起動)がファイル不在エラーにならない。

## タスク2 (P0-2): 常駐の自動復帰(2026-07-16 13:17の再発防止)

**問題**: PC再起動/ロックで常駐(poller/watchdog/responder/gemini/heartbeat)が全滅しても自動復帰しない(winupdate_watch.ps1:3-7に実害記録)。revive対象は現状Labセッションのみ。参照される`go5_daemons_hidden`タスクの定義もリポジトリに無い。

**手順**:
1. `schtasks /query /fo list | findstr /i "go5"` で既存タスクを棚卸(go5_lab_revive / go5_daemons_hidden 等の実在・対象・トリガーを記録)
2. 常駐全種をカバーする登録スクリプト(ログオン時自動起動+死活監視)を作成し**リポジトリへコミット**(scripts/_daemons/ 配下)
3. 二重起動ガード(設計書ops-4)を同時に入れる(pidファイル等。register前に既存プロセス確認)

**受け入れ条件**: `schtasks /query`で全常駐のタスクが確認できる+手動killから10分以内に自動復帰する実測+登録スクリプトがrepoにある。

## タスク3 (P0-5): PC上の未コミット運用スクリプトの棚卸とlocal/再構築手順

**問題**: supervise_daemons等の運用系PSスクリプトがリポジトリ外(PC喪失=再構築手順ごと喪失)。local/の秘密も単一コピー。

**手順**:
1. `scripts/_daemons/` 相当のPC上の実体と、PC上にしかない運用スクリプトを棚卸→秘密を除いてコミット
2. `docs/設計・調査/local再構築チェックリスト.md` を作成: local/配下の各ファイル(discord_bot_token/gemini_api_key/discord_channels.json/webhookキャッシュ/state類)について「何か・消えたらどう再発行/再生成するか」を1行ずつ
3. INC-95のバックアップ経路にlocal/の主要ファイルが含まれるか実測し、含まれなければ追加

**受け入れ条件**: チェックリストだけを頼りに(記憶なしで)localを再構築できる記述レベル。バックアップ実測結果の追記。

## タスク4 (P0-3残): 既存bat 12本のCRLF化(要・実行テスト)

**問題**: 監査でLF改行のbatが12本発覚(一覧=`tests/test_bat_encoding.js` の KNOWN_VIOLATIONS)。INC-58型の文字化けリスク。クラウドからは「変換後にcmdで1回実行して確認」ができないため盲目変換を見送った。

**手順**(`.gitattributes`の`*.bat text eol=crlf`が入ったため、CRLF化はgitに任せるのが安全):
1. `git pull` 後、対象batを **`del <bat>` → `git checkout -- <bat>`** で再生成(チェックアウト時にgitが自動でCRLF化する。手で変換しない)
2. 非ASCII(日本語)を含むbatは `@echo off` 直後に `chcp 65001 >nul` を追記(例=起動_go5-maker.bat)
3. **`cmd /c <bat>` で1回実行して動作確認**(INC-58の教訓・これはPCでしかできない)
4. 確認できたものから `tests/test_bat_encoding.js` の KNOWN_VIOLATIONS から該当行を削除(修正済みはINFO表示が出る)

**受け入れ条件**: `node tests/test_bat_encoding.js` が KNOWN_VIOLATIONS空の状態で全PASS。

---

## 補足(前倒し推奨・P2)
- `scripts/status.py`(常駐の死活を1コマンド表示): タスク2の受け入れ検証にそのまま使えるため、先に作ると効率的(設計書ops-5)。
