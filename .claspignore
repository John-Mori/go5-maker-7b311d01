# clasp が push する対象のホワイトリスト。
# リポジトリのソースは .gs（clasp はサーバースクリプトを .js で扱うが、本リポジトリは .gs を正とする）。
# push はクラウドをローカルのミラーに置き換えるため、実体（appsscript.json＋各スクリプトの .gs）だけを許可する。
**/**

!appsscript.json
!コード.gs
!snapshot.gs
